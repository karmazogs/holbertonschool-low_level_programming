#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - prints stuff about n
 * Return: 0
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	printf("Last digit of %i is %i", n, n % 10);

	return (0);
}
